# Ethical OSINT Tracker - Flask Dependencies

# Core framework
flask>=3.0.0
flask-login>=0.6.3

# Database
sqlmodel>=0.0.21
PyMySQL==1.1.1

# Authentication & Security
argon2-cffi==23.1.0

# Testing
pytest>=7.0

# HTTP Client (socks extra for Tor/proxy support)
httpx[socks]>=0.23

# Image Processing
Pillow>=10.0.0

# Audio metadata
mutagen>=1.47.0

# PDF metadata
pypdf>=4.0.0

# Video/generic metadata
hachoir>=3.1.3

# DOCX metadata
python-docx>=1.1.0

# Excel metadata
openpyxl>=3.1.0

# DNS record resolution (optional — subdomain scanner falls back to socket if absent)
dnspython>=2.6.0

# PDF report generation
fpdf2>=2.7.0

# HTML parsing (dark web search result extraction)
beautifulsoup4>=4.12.0
